{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ESNext",                                  /* Definir a versão do JavaScript emitido. */
    "module": "commonjs",                                /* Definir o formato do módulo. Pode usar "ESNext" se estiver usando import/export. */
    "baseUrl": "./",                                     /* Definir o diretório base para resolução de módulos. */
    "paths": {
      "src/*": ["src/*"]
    },                                                   /* Corrigido o erro de sintaxe no paths. */

    /* Modules */
    "moduleResolution": "node",                          /* Define como o TypeScript deve resolver módulos. */
    "esModuleInterop": true,                             /* Emitir código adicional para compatibilidade com módulos CommonJS. */
    "forceConsistentCasingInFileNames": true,            /* Assegura que o casing está correto nos imports. */

    /* Type Checking */
    "strict": true,                                      /* Habilitar todas as opções de verificação estrita. */

    /* Emit */
    "skipLibCheck": true,                                /* Ignorar verificação de tipos em arquivos de declaração. Pode acelerar a compilação. */
    "noEmitOnError": true,                               /* Não emitir arquivos se houver erros de tipo. */
    "sourceMap": true,                                   /* Gerar arquivos de mapa de código para depuração. */
    
    /* JSX */
    // "jsx": "react",                                   /* Ativar se estiver usando JSX no React. */

    /* JavaScript Support */
    "allowJs": true,                                     /* Permitir arquivos JavaScript no projeto. */
    // "checkJs": true,                                  /* Ativar relatórios de erro em arquivos JavaScript verificados. */
    
    /* Other Options */
    "resolveJsonModule": true                            /* Permite importação de arquivos JSON. */
  },
  "include": [
    "src/**/*"                                           /* Incluir todos os arquivos TypeScript dentro da pasta src. */
  ],
  "exclude": [
    "node_modules",                                      /* Excluir a pasta node_modules. */
    "**/*.test.ts",                                      /* Excluir arquivos de teste. */
    "**/*.spec.ts"
  ]
}
